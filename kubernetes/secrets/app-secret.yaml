# Sample secret manifest for demo purposes only; replace with sealed secrets or external secret integration in production.
apiVersion: v1
kind: Secret
metadata:
  name: platform-web-secrets
  namespace: platform-demo
type: Opaque
stringData:
  API_KEY: "replace-with-secure-secret"
  DATABASE_URL: "postgres://appuser:change-me@postgres.platform-demo.svc.cluster.local:5432/appdb"


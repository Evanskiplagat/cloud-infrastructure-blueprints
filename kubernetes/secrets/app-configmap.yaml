# Store non-sensitive runtime configuration separately from the container image.
apiVersion: v1
kind: ConfigMap
metadata:
  name: platform-web-config
  namespace: platform-demo
data:
  APP_ENV: "dev"
  LOG_LEVEL: "info"
  FEATURE_FLAG_SAMPLE: "true"


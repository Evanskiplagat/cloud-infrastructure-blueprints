# Example secret shape for local testing only. Do not commit real values to Git.
apiVersion: v1
kind: Secret
metadata:
  name: platform-web-secrets
  namespace: platform-demo
type: Opaque
stringData:
  API_KEY: "set-locally-only"
  DATABASE_URL: "set-locally-only"

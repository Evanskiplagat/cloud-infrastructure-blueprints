# Grafana Dashboard Examples

The JSON file in this directory provides a starter dashboard for AKS and application workload telemetry.

Import the dashboard after connecting Grafana to Prometheus and adjust the datasource name if your environment does not use the default `Prometheus` label.


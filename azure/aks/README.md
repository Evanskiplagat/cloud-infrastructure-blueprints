# Azure AKS Bicep Example

This example deploys an AKS cluster with a system node pool, managed identity, Azure RBAC, OIDC issuer support, and workload identity enabled.

Use it as a native Azure alternative to the Terraform-based AKS blueprint under `terraform/aks-cluster`.

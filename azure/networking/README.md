# Azure Networking Bicep Example

This folder contains a simple Bicep example for creating a virtual network, application subnet, AKS subnet, and a network security group.

The example is intentionally compact and suitable for demonstrating Azure-native deployment skills alongside Terraform.

